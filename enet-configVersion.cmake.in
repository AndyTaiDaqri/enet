set(PACKAGE_VERSION "@enet_VERSION@")
set(PACKAGE_VERSION_MAJOR "@enet_VERSION_MAJOR@")
set(PACKAGE_VERSION_MINOR "@enet_VERSION_MINOR@")
set(PACKAGE_VERSION_PATCH "@enet_VERSION_PATCH@")

if(PACKAGE_VERSION_MAJOR VERSION_LESS PACKAGE_FIND_VERSION_MAJOR OR
  PACKAGE_VERSION_MINOR VERSION_LESS PACKAGE_FIND_VERSION_MINOR OR
  PACKAGE_VERSION_PATCH VERSION_LESS PACKAGE_FIND_VERSION_PATCH)
  set(PACKAGE_VERSION_COMPATIBLE FALSE)
else()
  set(PACKAGE_VERSION_COMPATIBLE TRUE)
  if (PACKAGE_VERSION VERSION_EQUAL PACKAGE_FIND_VERSION)
    set(PACKAGE_VERSION_EXACT TRUE)
  endif()
endif()


